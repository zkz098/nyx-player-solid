name = "nyx-player-docs"
compatibility_date = "2026-08-22"

# Workers Static Assets — pure static (no SSR), Astro Starlight output
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
